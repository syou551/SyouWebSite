import { initSync, init } from '../../../wasm/pkg/foras.mjs';
export { DeflateDecoder, DeflateEncoder, GzDecoder, GzEncoder, ZlibDecoder, ZlibEncoder, deflate, gunzip, gzip, inflate, unzlib, zlib } from '../../../wasm/pkg/foras.mjs';
import { wasm } from '../../../wasm/pkg/foras.wasm.mjs';

let output = undefined;
function initSyncBundledOnce() {
    return output ??= initSync(Buffer.from(wasm, "base64"));
}
async function initBundledOnce() {
    return output ??= await init(Buffer.from(wasm, "base64"));
}

export { init, initBundledOnce, initSync, initSyncBundledOnce };
//# sourceMappingURL=index.mjs.map
