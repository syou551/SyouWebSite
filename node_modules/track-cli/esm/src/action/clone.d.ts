export interface CloneOptions {
    targetDir?: string;
    basicAuthUser?: string;
    basicAuthPassword?: string;
}
export declare function clone(url: string, options?: CloneOptions): Promise<void>;
